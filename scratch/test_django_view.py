import requests

url = "http://localhost:8000/api/pagos/qr-presencial/"
# Need to login or pass JWT token? 
# The view requires IsAuthenticated. Let's just mock the auth or use the test script with JWT token.
